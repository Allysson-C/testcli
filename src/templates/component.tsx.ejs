import React from 'react';
import classNames from 'classnames';

import styles from './<%= props.name %>.module.scss';

export function <%= props.name %>(){
  return (
    <h1 className={styles.h1}>Hello <%= props.name %></h1>
  )
}